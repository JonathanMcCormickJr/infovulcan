/target/
**/*.rs.bk

# Generated PKI — gen-certs.sh writes a CA + service private keys here. Never commit keys.
/certs/

# Coverage artifacts (regenerated by scripts/coverage.sh / cargo tarpaulin)
/cobertura.xml
/lcov.info
/tarpaulin-output/

# Note: Cargo.lock MUST be committed for reproducible builds and security audits